<body id="form">
  <header>
    <nav id="nav-bar">
      <a href="/"> ← Go back {{user.username}}</a>
    </nav>
  </header>
<div>
    {{#with room}}
      <div class="room-card-expanded">
        <div class="card-expanded-container-room">
          <img width= "250" height="250" src="{{imageUrl}}" alt="{{name}}"/>
        </div>  
        <div class="card-container">
          <h2>{{name}}</h2>
          <p>Description:<br>{{description}}</p>
          <p>Owner:<br>{{owner.username}}</p>
          <div id="button-see-more">
            {{#if user.username}} {{!-- This is nor working. Need to check --}}
              <a href="/rooms/{{_id}}/edit">Edit Room</a>
              <a href="/rooms/{{_id}}/delete">Delete Room</a>
            {{/if}}
          </div>
        </div>
     </div>
    {{/with}} 
  </div>
</body>